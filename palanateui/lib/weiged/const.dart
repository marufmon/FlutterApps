import 'package:flutter/material.dart';

myStyle(double? size, Color? clr, [FontWeight? fw]) {
  return TextStyle(
    color: clr,
    fontSize: size,
    fontWeight: fw,
  );
}

Color cardColor = Color(0xff292B3E);
