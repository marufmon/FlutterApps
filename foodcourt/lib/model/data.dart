class productList {
  String title;
  String subtitle;
  String image;

  int quantity;
  int prices;
  int totalPrice;

  productList(this.title, this.subtitle, this.image, this.quantity, this.prices,
      this.totalPrice);
}
